apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: uclab-wildcard-tls
  namespace: gateway-system
spec:
  secretName: uclab-wildcard-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "*.uclab.dev"
