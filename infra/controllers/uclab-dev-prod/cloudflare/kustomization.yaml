apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cloudflared
resources:
  - ../../base/cloudflare
  - tunnel-secret.yaml

configMapGenerator:
  - name: config
    namespace: cloudflared
    files:
      - config.yaml

generatorOptions:
  disableNameSuffixHash: true
