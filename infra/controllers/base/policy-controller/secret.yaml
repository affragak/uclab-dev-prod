# Registry credentials for the webhook to pull image manifests + cosign
# signatures from the private Forgejo registry during verification.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-registry
  namespace: cosign-system
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend-global
    kind: ClusterSecretStore
  target:
    name: forgejo-registry
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {"auths":{"{{ .server }}":{"username":"{{ .username }}","password":"{{ .password }}","auth":"{{ printf "%s:%s" .username .password | b64enc }}"}}}
  data:
    - secretKey: username
      remoteRef:
        key: forgejo-registry
        property: username
    - secretKey: password
      remoteRef:
        key: forgejo-registry
        property: password
    - secretKey: server
      remoteRef:
        key: forgejo-registry
        property: server
