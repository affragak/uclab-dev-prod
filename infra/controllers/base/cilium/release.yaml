apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cilium
  namespace: flux-system

spec:
  targetNamespace: kube-system
  releaseName: cilium
  interval: 15m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: cilium
      version: 1.20.1
      sourceRef:
        kind: HelmRepository
        name: cilium
        namespace: flux-system

  valuesFrom:
    - kind: ConfigMap
      name: cilium-values