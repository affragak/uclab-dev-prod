apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hugoblog
  namespace: hugoblog
spec:
  parentRefs:
    - name: uclab-gateway
      namespace: gateway-system
  hostnames:
    - uclab.dev
  rules:
    - backendRefs:
        - name: hugoblog
          port: 80
