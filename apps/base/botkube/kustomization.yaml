apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - release.yaml
  - storage.yaml
configMapGenerator:
  - name: botkube-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
