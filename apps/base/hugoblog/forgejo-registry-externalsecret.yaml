# Pull secret for the Forgejo container registry (forgejo.uclab.dev), which
# the blog's Forgejo CI now builds/pushes to. Synced from Vault key
# forgejo-registry (server/username/password).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-registry
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend-global
    kind: ClusterSecretStore
  target:
    name: forgejo-registry
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {"auths":{"{{ .server }}":{"username":"{{ .username }}","password":"{{ .password }}","auth":"{{ printf "%s:%s" .username .password | b64enc }}"}}}
  data:
    - secretKey: username
      remoteRef:
        key: forgejo-registry
        property: username
    - secretKey: password
      remoteRef:
        key: forgejo-registry
        property: password
    - secretKey: server
      remoteRef:
        key: forgejo-registry
        property: server
